Javascript and JSON

Globaly installing gulp:

npm install -g gulp

Globally install Browserify

npm install -g browserify

Installing sass:
gem install sass

Instaling compass:
gem install compass

Updating components:
gem update
